# 记忆台账索引

> 唯一入口：一行一条 + 链接，按 description 判断相关性再展开。不要一次性读整个 `memory/` 目录。

（索引，按 §4.4 逐行追加：`- [[<name>]] —— 一句话说明 + 何时读它`）

- `[[制作约束]]` —— 视频制作必保行为红线（真人原片只读、字幕跟随真实音频、PIP/暂停规则、不虚构证据、代理/抽帧/渲染分离、手写镜头禁止套demo卡、出小样前过§5清单）；每次动素材或做视频前必读
- `[[第17期-动效返工]]` —— 套demo卡方向错 + 人物一直展示 + 小样太长 + 不读demo TSX就手写简化动效（100%禁止）；做动效/出小样前必读，防再犯
- `[[第17期-文档版本化堆积与重复读取]]` —— 每版小样各写一份对照文档 + 开工重读历史版本；写/改实现记录前必读（只保留一份无版本号当前生效文档，废弃进 archive/）
- `[[第17期-python脚本批量清理破坏结构]]` —— 用 python/sed 批量替换清理套壳时误删 GrokHook 片头人物 pip 容器尺寸导致人物消失；批量替换前必读（逐一确认每处匹配上下文语义一致）
- `[[第17期-特效补充轮计划偏离与简化动效]]` —— 执行 12 特效时偏离既定计划（特效位置/章节标题/转场数量）、未确认就改全片时长、circle-match-iris 跳景A脉冲、demo 物理参数未按坐标系换算、找 demo 凭分类猜路径；批量加特效前必读
- `[[剪辑-特效密度与多样性-2026-09-17]]` —— 特效必须一句/半句一个、全片不重复用同一张卡（库有152张）；照旧模板「2-3句一个节点」做会被用户打回；做任何一期动效/特效映射前必读
- `[[剪辑-remotion-trimBefore动态帧号导致二倍速]]` —— Video 的 trimBefore 传当前帧号会让视频 2 倍速播放；人物 PIP/视频轨道必须传固定起始帧；改人物口播/音画对齐前必读
- `[[剪辑-remotion-封面裁剪焦点对齐露黑边]]` —— 人物 PIP/竖屏框 cover 裁剪时 left/top 不 clamp 到有效范围会露黑边（正方形框高度刚好 fit、焦点向下偏移→顶部露黑框）；改人物 PIP/竖屏裁剪前必读
- `[[framesbase-设计资料库接入]]` —— Framesbase 私人设计资料库 MCP 已接入（本地客户端脚本，网站/应用/区块设计 prompt 库，非视频特效库；搜索只命中标题/分类名）；要查/复用 Framesbase 设计参考时读
- `[[kling-图生视频接入]]` —— 可灵 Kling 图生视频已接入（`skills/kling/` 本地 CLI，静态照片→运镜视频，Bearer 鉴权，base64 传图不加 data: 前缀，生成视频 30 天清理需立即转存）；要把猫/产品照片“动起来”生成视频素材时读
- `视觉参考与风格库` —— 已核实剪辑细则、风格库与特效模板，入口见 `memory/reference/README.md`；素材复核通过、进入包装阶段后读。其中 `AI剪辑参考会话沉淀.md` 与 `特效使用模板.md` 为每期必读（workflow.json read_order 与 AGENTS.md 开工必读已强制点名）
- `第6期黄金参考` —— 用户认可的第6期 workbuddy 成片方法论；开头四段编排与转场基线已提升到 `skills/video-packaging-structure/references/episode6-opening-baseline.md`、`episode6-transition-baseline.md`，做动效/开头前必读；源头分析见 `视频项目/第6期 workbuddy/08-GoldenReference/`（仅用户明确指向第6期时才读该目录）
- `踩坑/常见问题` —— 反馈技巧、制作链断层、QA、原则与常见问题，入口见 `memory/pitfalls/README.md`；踩到问题或给反馈前读
