# 我如何用 Codex 完成一条真人口播视频：从粗剪、包装方案到最终成片

> 使用 `video-use + video-shotcraft + Remotion`，把已经粗剪完成的真人口播视频，逐步制作成带字幕、录屏、产品素材和动效包装的完整视频。

GitHub 项目：[codex-video-production-workflow](https://github.com/SuperYu-simiao/codex-video-production-workflow)

很多人第一次让 Codex 剪视频，会直接发一句：

> 帮我把这条视频剪好，加字幕、录屏和一些高级的动效。

这样的提示词看起来很省事，但实际结果往往不稳定：动效和口播对不上、人物小窗切到脸、录屏里的文字看不清，甚至改完一轮以后，原本已经满意的效果也被一起改掉。

我后来发现，问题并不是 Codex 不会写 Remotion，也不是缺少特效，而是没有把视频制作拆成正确的协作阶段。

我现在使用的完整流程是：

```text
整理素材
→ 分析口播与字幕
→ 设计包装方案
→ 补录操作素材
→ 制作开头小样
→ 根据截图逐点反馈
→ 确认视觉基线
→ 扩展到全片
→ 技术与画面 QA
→ 最终成片
```

这篇教程会把这套方法完整拆开，并提供每个阶段可以直接复制给 Codex 的提示词。

---

## 一、这套流程适合什么视频

这套流程主要适合：

- 真人口播教程；
- AI 工具、软件和产品介绍；
- 需要穿插操作录屏的视频；
- 需要展示官网、GitHub、Logo、提示词或流程图的视频；
- 希望形成固定个人风格、以后可以反复复用的视频。

本文主要讲的是“口播精剪之后的画面包装”。

最理想的输入是一条已经处理完口误、重读、卡顿和错误镜头的真人口播视频，以及一份与它完全对应的 SRT 字幕。如果你拿到的还是相机原片，可以先用 `video-use` 完成转录和口播精剪，再进入本文的包装阶段。

### 最低素材要求

至少准备下面两项：

1. 已经粗剪完成的真人口播视频；
2. 与视频完全对应的 UTF-8 编码 SRT。

如果还有下面这些素材，最终效果会更完整：

- 产品或软件操作录屏；
- 手机端操作录屏；
- 官网、GitHub 首页或官方 Logo；
- 产品结果截图；
- 需要展示的命令、代码和提示词原文；
- 已获授权的音乐、音效与图片；
- 一份你喜欢的视觉参考或个人风格规范。

### 为什么 SRT 很重要

SRT 不只是用来显示字幕，它还是画面包装的语义时间轴。

Codex 可以根据每段字幕的文字和时间码判断：

- 哪句话正在介绍工具；
- 哪句话进入新章节；
- 哪句话在讲步骤；
- 哪个位置应该展示录屏；
- 哪个位置适合暂停口播并插入转场；
- 后续画面和字幕应该怎样同步。

如果粗剪视频再次发生删改，必须重新导出 SRT。旧时间码不能继续沿用。

---

## 二、三个工具分别负责什么

这套流程不是让一个工具包办所有工作，而是让三个部分各自负责擅长的事情。

### 1. video-use：导演与剪辑时间线

`video-use` 负责理解口播内容、转录、口播精剪和导演时间线。

到了包装阶段，它还需要决定：

- 每段口播对应什么画面；
- 真人应该全屏、靠左、靠右还是缩成小窗；
- 录屏和产品素材从哪里进入；
- 动效结束后从哪个源视频时间点继续；
- 字幕、素材、音乐和特效在成片时间线上怎样对齐。

它更像整条视频的导演和剪辑指导。

### 2. video-shotcraft：经过设计的镜头配方

`video-shotcraft` 提供的不是简单的“特效名称”，而是一套完整镜头配方，包括：

- 构图；
- 动画顺序；
- 缓动和时值；
- 景深与运动模糊；
- 对应的 Demo TSX；
- 适合出现在哪类口播语义里。

正确使用方法不是看到一个 `style-key` 后重新手写一个相似动画，而是读取对应的完整卡片和准确 Demo，再替换成当前视频的文字、Logo、截图和录屏。

### 3. Remotion：按照时间线完成合成

Remotion 负责把已经确认的内容合成到一起：

- 真人视频轨；
- 人物遮罩或小窗轨；
- 背景轨；
- 录屏和图片轨；
- 动效轨；
- 字幕轨；
- 音乐和音效轨。

Remotion 不应该自行决定剪辑节奏，也不应该自己发明画面文案。画面中出现的文字，应该来自逐字稿、真实素材或已经确认的内容。

所以整条制作链可以概括为：

```text
video-use 做导演和时间线
→ video-shotcraft 提供准确镜头配方
→ Remotion 按确认后的轨道完成合成
```

---

## 三、第一次使用：先检查环境，不要立即安装

下载或克隆工作流项目后，在下面这个目录中打开 Codex：

```text
<仓库路径>/codex-video-production-workflow
```

第一次使用时，先让 Codex 只做环境检查：

```text
请使用 $video-production-bootstrap 检查这个本地项目的视频环境。

请先读取：
- 00-先读我.md
- workflow.json
- dependencies.json

先只报告缺失项，不要安装、创建项目、剪辑或渲染。
```

Codex 会检查 Git、Node.js、npm、Python、FFmpeg、ffprobe 以及工作流依赖。

只有确认缺失项以后，再单独授权安装。环境检查通过不代表已经授权剪辑，安装完成也不代表已经授权渲染。

不要把 API Key 直接粘贴到聊天或截图里。密钥只放在本机环境变量或不会提交的 `.env` 文件中。

---

## 四、每一条视频都要建立独立项目

不要把不同视频的口播、字幕、录屏和 Remotion 工程混在一起。

开始一条新视频时，可以运行：

```bash
python3 .agents/skills/video-production-bootstrap/scripts/create_video_project.py \
  --name "产品功能演示"
```

项目会包含下面这些阶段目录：

```text
01-原始素材/
02-转录与剪辑决策/
03-精剪输出/
04-动效与包装/
05-Remotion工程/
06-预览与审核/
07-成片/
```

把原始输入放进 `01-原始素材/`：

```text
01-原始素材/
├── 01-口播视频/
├── 02-录屏/
├── 03-图片与截图/
├── 04-音频/
├── 05-文稿与字幕/
├── 06-授权说明/
└── 07-背景与动效素材/
```

原始素材应该始终保持只读。Codex 产生的转录、剪辑决策、预览和成片分别写入后续目录，不覆盖相机原片和原始录屏。

---

## 五、第一轮只做素材盘点和补录清单

把 Word 逐字稿放在当前项目根目录、与 `project.json` 同级。素材放好以后，不要马上让 Codex 开始做视频。

第一轮只让它理解口播、检查素材并列出缺口。不要在素材未知时消耗上下文设计包装：

```text
请按《视频制作统一工作流》执行。

当前视频项目：
<当前项目绝对路径>

项目里已经有：
1. 与 project.json 同级的 Word 逐字稿
2. 尚未在这一阶段读取的真人口播视频
3. 已有录屏、图片和音频素材
4. 已有的参考资料

请先读取 Word 逐字稿，并按文件名和已有台账盘点其他素材，只分析为了讲清楚内容还缺什么。不要打开、播放、抽帧、提取音频、转录或 ffprobe 分析口播视频。

这轮只输出以下内容：
1. 需要补录的操作素材清单
2. 公开图片、官网、GitHub 和 Logo 的收集项
3. 每项对应的口播原句、用途、来源、录法、规格、文件名和验收标准
4. 素材收集台账及当前完成／缺失／不可用状态

清单注明“定位依据：Word逐字稿”“时间码状态：未对音频核验”。不要设计镜头、人物构图、特效、转场或成片秒数，也不要制作小样、剪辑或渲染视频。
```

这一轮完成后，`04-动效与包装/` 中先出现两类文件：

- 需要补录的操作素材；
- 素材收集台账。

先把“需要什么证据、谁来取得、怎样算可用”说清楚，素材齐后再统一设计包装。

### 包装阶段的动效必须跟着语义，而不是跟着秒数

下面是我自己的常用映射，可以作为示例：

| 口播语义 | 对应画面或特效 |
| --- | --- |
| 进入新章节或小标题 | `aurora-bloom-bg-flip` |
| 第一次提到工具或 Skill | `assemble-then-type-flyin`，右侧展示官网、GitHub 或 Logo |
| 讲解多个步骤 | `autolayout-gap-dial` |
| 关键问题后的暂停转场 | `card-flock-tumble + 短 BGM` |
| 开头需要快速丰富画面 | `graze-face-tour`、`flying-words` 或透明后景文字层 |
| 展示可复制提示词或命令 | 文档式逐字显示，并保留原文、换行和缩进 |

这些不是必须套用的固定模板。真正的原则是：画面必须帮助观众理解正在说的内容。

我通常要求大约每两到三句话出现一次明确的画面变化，但不会每句话都切屏。主动效结束后还要留出稳定阅读时间。

---

## 六、让 Codex 把补录需求写到可以直接照着录

“这里需要一段录屏”并不是一份合格的补录清单。

每一条补录任务至少应该写清楚：

- 对应逐字稿中的哪句话；
- 从哪个界面开始；
- 执行哪些操作；
- 录到哪个结果结束；
- 建议时长；
- 是否保留鼠标轨迹；
- 是否需要展示输入过程；
- 哪些用户名、私人路径、项目名和密钥需要遮挡。

可以继续发送；这仍属于素材准备：

```text
请把补录素材清单继续细化。

每条录屏任务必须包含：
1. 对应的逐字稿原句
2. 录制起点和终点
3. 具体操作步骤
4. 建议时长和画面比例
5. 鼠标、键盘输入和停留要求
6. 需要脱敏的内容
7. 素材要证明或说明什么，以及怎样算录制完成

请把真实操作录屏与官网、GitHub、Logo 等公开身份素材分开列出。
公开静态素材按我已授权的范围由你保存并记录来源；需要动态录屏的网页给我准确链接和操作步骤，由我录制。
这轮不设计包装或制作视频。
```

### 公开素材和私人录屏要分开

官网首页、公开 GitHub 仓库和官方 Logo 可以作为工具身份素材，但登录后的后台、个人项目和本地文件路径不能直接当作公开素材。

录屏前要特别检查：

- 用户名和头像；
- 本地绝对路径；
- API Key、Token 和邮箱；
- 私人项目名称；
- 客户数据；
- 浏览器书签、通知和历史记录。

### 素材齐后再统一设计包装

用户补录回来后，先复核内容、画质、可用区间、来源和脱敏情况。只有叙事必需素材已经齐备，或用户明确接受缺项及诚实替代，才读取 Shotcraft、视觉风格和特效规范，一次生成同版本的包装方案、更新后的补录清单、公开素材表和特效映射。历史案例中的特效次数和秒数用于理解选择逻辑，不是新视频的固定配额。

---

## 七、不要直接做全片，先做一个开头小样

包装方案通过后，我不会直接让 Codex 做完整视频，而是先做一个二十到三十秒的开头小样。

这个小样最好同时覆盖：

- 全屏真人；
- 真人与透明后景动效；
- 工具身份卡；
- 全屏录屏与人物小窗；
- 步骤动画；
- 一次完整转场。

可以使用下面的提示词：

```text
包装方案方向通过。

现在只制作 B01—B03 的 20—30 秒开头小样，输出到 06-预览与审核/。

要求：
1. 使用真实高清口播源，不使用低清代理判断最终画质
2. 动效必须绑定真实口播时间
3. 使用 Shotcraft 卡片对应的准确 Demo 和运动参数
4. 展示录屏时，真人缩到右上角 1:1 正方形小窗
5. 人物必须按脸部和身体中心重新裁切，不能只做几何居中
6. 字幕始终属于主画面，避开人物、录屏和重要文字
7. 暂停转场必须真正暂停源视频，结束后从同一个源帧继续
8. 只做开头小样，不制作全片，不输出最终成片

渲染后请抽取代表帧，检查人物裁切、字幕、录屏可读性和转场接缝。
```

### 为什么一定要先看小样

写在方案里的“科技感”“快速”“透明”“有层次”，每个人理解都不同。

只有看到真实视频以后，你才能判断：

- 动效速度是不是太慢；
- 图层是不是挡住人物；
- 两个卡片是否同时弹出得太突兀；
- 录屏是否足够大；
- 人物小窗是否切到脸；
- 字幕是否与画面抢信息；
- 这套视觉语言是否真的像你的内容。

小样不是缩小版成片，而是用最低成本确认全片视觉规则。

---

## 八、怎样给 Codex 有效反馈

“不好看”“不够高级”“再丰富一点”都很难直接转化为可靠修改。

我现在会按下面四项反馈：

```text
时间点或截图：
具体问题：
希望变成什么效果：
哪些部分保持不变：
```

例如：

```text
时间点：00:02—00:05

具体问题：
前几秒动效飞入太慢，多个图层一起出现，显得拖沓。

希望效果：
改成约 1 秒切换一种运动语言，飞入更快、更绚烂；
透明文字放到人物后面，不要挡住脸和字幕。

保持不变：
人物口播、字幕位置和黑灰背景保持不变。
```

如果是根据截图反馈，可以直接把截图附给 Codex，并在图片上圈出问题区域。

### 一次只校准一类问题

为了避免改动互相干扰，我通常分开处理：

1. 先调开头节奏；
2. 再调人物构图和小窗；
3. 再调工具卡和录屏大小；
4. 最后调字幕、颜色和局部细节。

每一轮都明确哪些部分已经通过、不能再被修改。

例如：

```text
V3 的开头节奏已经通过，请保持不变。

这轮只修改录屏出现时的人物小窗：
1. 改为 1:1 正方形
2. 按脸部和身体中心重新取景
3. 保持在右上角安全区
4. 不得遮挡标题、按钮和字幕

请不要修改已经确认的开头动效、背景、字体和音乐。
```

---

## 九、做出来不像参考视频时，不要继续盲调

有时 Codex 使用了相同的特效名称，结果却和之前确认过的视频完全不同。

这通常不是颜色或速度的问题，而是制作链中的某一层被省略了。

这时先让 Codex 做差异审计：

```text
这版和我之前确认过的效果不一致。

请先只做差异审计，不修改视频：

1. 是否建立了真实口播对齐的导演时间线
2. 是否读取了每张 Shotcraft 卡的完整说明和准确 Demo
3. 是否复用了已经确认过的 Remotion 组件
4. 是否把导演备注误做成了观众画面文案
5. 暂停转场是否真正暂停源视频并补偿后续时间线
6. 人物小窗是否按人物中心重新裁切
7. 当前小样是否直接读取高清源素材

请明确告诉我缺失的是哪一层，再提出修改方案。
这轮只诊断，不要渲染。
```

### 常见的制作链断层

#### 只记住特效名，没有复用准确实现

`card-flock-tumble`、`assemble-then-type-flyin` 等名称只是入口。重新写一个“差不多”的动画，运动顺序、缓动、层级和阅读节奏都会发生变化。

#### 没有真实口播时间线

按照逐字稿字数比例估算时间，只适合前期规划，不能直接驱动正式小样。实施前必须对齐真实语音或字幕时间。

#### 把导演备注放进了画面

“这里展示步骤”“提到工具名就给它证据”属于制作说明，不是观众应该看到的文案。

#### 假暂停

只把声音静音、再盖上一张定格画面，但底层源视频还在继续播放，不是真正的暂停。转场结束后会跳过一段口播。

正确方法是拆开源视频轨，在插入段结束后从同一个源帧继续，并统一补偿后续字幕、素材和特效的位置。

---

## 十、小样确认后再扩展到全片

当开头小样的节奏、构图、人物裁切、录屏样式和字幕规则都通过后，再让 Codex 把这套视觉语言扩展到完整视频。

```text
开头小样 V3 的视觉方向通过。

请以这一版为全片视觉基线继续制作：
1. 保留已经确认的组件、人物构图和运动参数
2. 逐句绑定真实口播时间
3. 优先使用项目中已有的真实录屏和截图
4. 人物缩小时始终按人物中心重新裁切
5. 导演备注不得显示在观众画面中
6. 每个主要动效结束后保留稳定阅读时间
7. 先输出全片审核版到 06-预览与审核/
8. 暂时不要写入 07-成片/

完成后请抽取开头、中段、录屏段、复杂转场和结尾的代表帧，并提供 QA 记录。
```

这里要注意：

- “包装方案通过”只代表可以准备小样；
- “开头小样通过”只代表可以扩展全片；
- “全片审核版通过”才代表可以执行最终渲染；
- 安装、录屏、预览、最终渲染和上传是不同授权。

---

## 十一、最终成片前必须做 QA

不要只看视频能否正常播放。

### 技术检查

- 分辨率和画面比例是否正确；
- 帧率是否与工程设置一致；
- 视频和音频是否可以完整解码；
- 音频采样率、声道和响度是否正常；
- 是否存在持续黑帧或空白画面；
- 色彩范围是否正确；
- 最终时长是否符合时间线计算。

### 字幕检查

- 字幕是否与口播同步；
- 是否有错字、漏字和错误技术名词；
- 是否被录屏、动效或人物挡住；
- 是否进入人物脸部、按钮、表格或代码区域；
- 切掉口播以后，字幕时间是否同步更新。

### 画面检查

- 人物小窗是否切到脸、手势或身体；
- 人物是否在正方形小窗中真正居中；
- 录屏、命令和表格能否在正常播放速度下看清；
- 动效结束后是否留下足够阅读时间；
- 转场前后是否出现残影、重叠标题或空舞台；
- 暂停转场结束后是否跳过口播；
- 是否意外显示用户名、私人路径、密钥或客户信息。

### 代表帧检查

至少抽取：

- 开头前两秒；
- 第一次工具卡；
- 第一次全屏录屏；
- 第一次人物小窗；
- 最复杂的转场；
- 视频中段；
- 最后两秒。

代表帧通过以后，再完整观看一遍审核版。

---

## 十二、最后单独授权最终渲染

只有全片审核版确认以后，再发送最终渲染指令：

```text
全片审核版通过，现在允许执行最终渲染。

请输出到 07-成片/，并完成以下检查：
1. 完整解码
2. 分辨率、帧率和时长
3. 字幕同步与字幕安全区
4. 音频响度和峰值
5. 黑帧与空白画面
6. 色彩范围
7. 隐私与敏感信息

不要上传或发布视频。
完成后请提供最终文件路径和 QA 报告。
```

如果还需要上传、发布或发送给别人，应该再次确认具体文件和具体目标，不要把“允许渲染”理解成“允许上传”。

---

## 十三、我从实际制作中总结出的几个原则

### 1. Codex 剪辑的关键不是一条超长提示词

真正有效的是分阶段协作：

```text
先盘点并列补录清单
→ 收集公开素材并完成用户录屏
→ 素材复核后统一规划
→ 再做小样
→ 根据画面反馈
→ 确认后扩展全片
```

### 2. 先确认视觉基线，再追求全片效率

如果开头二十秒的节奏和构图还没有确定，直接做三分钟全片，只会把错误复制到更多画面。

### 3. 反馈必须指向具体时间和具体图层

“高级一点”不是执行标准；“00:04 的右侧卡片延迟 0.3 秒进入，标题保持不变”才是。

### 4. 真实证据比装饰性动效更重要

介绍工具时，真实官网、GitHub、Logo、操作录屏和结果截图，比无关的光效更能建立可信度。

### 5. 动效是为了帮助理解

不是每句话都需要飞入、旋转或故障。该阅读的时候，画面应该停下来。

### 6. 已确认的版本要成为下一轮基线

不要让 Codex 每次都从特效名称重新猜。把已经确认的组件、节奏、人物构图和时间线作为下一轮明确输入。

---

## 十四、常见问题

### 这套流程会自动删除口误吗？

本文主要讲包装阶段。如果是相机原片，素材盘点时先确认是否需要转录和精剪；素材齐并锁定包装后，再按授权使用 `video-use` 对齐真实口播、处理口误并生成精剪副本和导演时间线。

### 没有 SRT 能不能做？

可以先转录生成字幕。但如果要让动效准确落在口播语义上，最好使用与最终粗剪完全对应的 SRT 或词级时间戳。

### 一定要使用相同的 Shotcraft 特效吗？

不需要。本文中的特效是我的个人视觉语法。你可以根据品牌和内容建立自己的映射，关键是先验证完整卡片和 Demo，不要只凭名称仿写。

### 为什么不直接让 Codex 做完整视频？

因为审美和构图必须通过真实画面校准。先做一个代表性小样，可以在最低成本下发现大部分系统性问题。

### 可以输出到其他剪辑软件继续修改吗？

可以。如果你需要传统时间线继续精修，可以把最终视频、字幕、分轨素材和剪辑决策交给支持的剪辑工具；需要原生可编辑项目时，应在开始前明确交付格式。

---

## 写在最后

Codex 真正改变我的，不是让视频制作变成“按一下按钮自动出片”，而是让我可以用自然语言完成原来需要在多个软件之间反复沟通的导演、剪辑和动效协作。

但自然语言协作并不意味着可以省掉制作流程。

越是复杂的视频，越要把“分析、方案、小样、反馈、全片和成片”分开。每一阶段都留下可以复查的文档、预览和确认结果，Codex 才能稳定地延续你的风格，而不是每一轮重新猜一次。

如果只记住一句话，可以记住这一句：

> 不要让 Codex 一上来就做完整视频。先把补录素材准备齐，再统一写方案，并用一个开头小样和具体反馈校准视觉语言。
