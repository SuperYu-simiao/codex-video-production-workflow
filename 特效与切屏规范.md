# 特效与切屏规范

这是一套在素材整理完成后使用的包装规则。素材准备期只做盘点、补录清单、公开静态素材收集和用户录屏指导，不提前用本文设计镜头或选择特效。素材就绪后，本文规定真人口播、标题舞台、录屏/截图和协作画布之间如何切换。

机器可读版本见 [`effects.json`](effects.json)。Codex 在设计分镜或制作动效前必须同时读取这两个文件。

从逐字稿语义、素材切入点和场景切换自动选特效的决策模板见 [`视觉规范与参考/特效使用模板.md`](视觉规范与参考/特效使用模板.md)。包装阶段设计分镜时，先按该模板从语义选主动效、定人物状态和转场，再落到本文的具体制作规范和 `effects.json` 的机器可读映射；不要逐句向用户要"这句话配哪个特效"。

## 特效库来源

本工作流的部分镜头结构、运动语法、节奏参数和 Remotion 实现参考自：

- GitHub：[Vincentwei1021/video-shotcraft](https://github.com/Vincentwei1021/video-shotcraft)
- 在线 Gallery：[video-shotcraft Motion Library](https://vincentwei1021.github.io/video-shotcraft/library.html)
- 固定版本：`0d6f0b57f0d4d6700761644c07f7ef03c3e50234`
- 许可证：Apache-2.0
- 默认安装位置：`~/.codex/skills/video-shotcraft`

该仓库是一个 Skill 和镜头配方能力库；当前固定版本说明包含 152 张镜头卡、209 条动态样片、对应 demo 源码、Remotion 模板和音频资产。本工作流只继承适合当前内容的镜头结构、运动语法、节奏和实现思路，不把 Gallery 样片当作用户素材直接使用。

Codex 使用其中任一效果时必须：

1. 在 `gallery/api/library.json` 校验准确的卡名和 `style-key`。
2. 完整读取对应的 `references/shots/.../*.md` 镜头卡。
3. 按镜头卡的“参考实现”读取准确的 demo TSX，不凭名称重新手写近似效果。
4. 用当前项目的文案、字体、颜色、截图、录屏和人物素材重新适配。
5. 检查素材授权、画面可读性、字幕避让和渲染稳定性。

## 五种画面状态

| 状态 | 何时使用 | 主画面 | 真人位置 |
|---|---|---|---|
| `talking-head-fullscreen` | 开场口播、个人观点、情绪强调、没有配套素材的解释 | 真人口播全屏 | 全屏 |
| `title-stage` | 说出产品名、工具名、章节名、关键结论 | 黑灰科技动态网格，叠加标题或关键词动效 | 右下角小窗 |
| `material-fullscreen` | 展示录屏、截图、图片、文档、案例证据 | 素材铺满全屏，关键 UI 完整可读 | 右下角小窗，必要时移到其他安全角 |
| `code-prompt-stage` | 口播给出可复制的代码、提示词、命令、配置或分步指令 | 大尺寸代码/提示词框，内容完整可读，可带类型标签、短标题和复制提示 | 右侧安全区小窗，不能挤压正文 |
| `split-dialogue-canvas` | 对话、协作、流程拆解、AI 生成到画布 | 分屏或全屏画布，内容关系优先 | 右下角小窗或暂时隐藏 |

默认从 `talking-head-fullscreen` 开始。产品名称、章节标题和关键观点出现时进入 `title-stage`；口播提到具体界面、操作、截图或文件时进入 `material-fullscreen`；给出观众需要照抄或复用的代码、提示词、命令、配置或指令时进入 `code-prompt-stage`；讲协作、交接或内容生成过程时可进入 `split-dialogue-canvas`。

## 真人小窗与字幕

- 真人小窗建议占画面宽度的 22%–28%，右侧和底部安全边距各约 4%。
- 小窗使用稳定的 16:9 或按人物构图确定的固定比例，圆角和描边全片一致。
- 小窗不能遮住按钮、输入框、数据、产品名、图表结论或字幕；冲突时优先移动小窗，其次调整字幕位置。
- 口播音轨连续保留。素材进入全屏时，把同一真人片段从全屏平滑缩放并移动到角落，不重新起一段人物视频。
- `talking-head-fullscreen -> PIP` 建议用 12–20 帧完成缩放、位移、圆角和描边显现；返回全屏时反向执行。转场必须在一句话的语义落点开始，而不是随意卡在句中。
- 字幕属于主画面合成层，绝不写进真人小窗。右下角有小窗时，字幕默认放在主画面左侧约 68% 的安全区；如果素材底部有重要 UI，则把字幕上移。

## 开头动效

开头可以按口播语义依次使用以下三个镜头，但不应为了凑效果强行全部使用：

1. `magician-card-flourish`：在钩子、核心对象或主视觉第一次出现时，把卡片/截图“变出来”。
2. `dashboard-glow-highlight-pill`：口播说到关键能力、重点字段或决定性短语时，用高亮 pill 锁定信息。
3. `neon-frame-orbit-drop`：需要从开场主视觉转入讲解状态时，把真人口播放入霓虹框，让人物随框体运动并最终落到右下角。

每个动效必须与对应口播词句对齐。动效结束后保留 0.8–1.5 秒稳定画面，让观众读完标题或看清主体。

## 用户默认包装结构

用户为后续视频指定了一套固定的包装骨架。它由 [`skills/video-packaging-structure/SKILL.md`](skills/video-packaging-structure/SKILL.md) 执行，机器可读映射见 `effects.json` 的 `default_packaging_structure`。这些规则先用于分镜和素材计划，不代表已经获得剪辑、采集、预览或渲染授权。

### 制作职责链与第五期基线

完整制作链固定为：`video-use 导演时间线 -> video-shotcraft 准确卡片与 demo -> Remotion 分轨合成`。实施小样前，先按 [`director-timeline-schema.json`](skills/video-packaging-structure/references/director-timeline-schema.json) 写入真实口播时间、源片/母版帧和插入补偿；逐字稿比例估时只可用于规划。用户选择风格库2时必须先读风格库2两份入口；用户要求延续第五期开头时，再读 [`episode5-opening-baseline.md`](skills/video-packaging-structure/references/episode5-opening-baseline.md) 并以已确认组件为基底。

暂停桥接必须拆开源视频轨：插入段结束后从同一源帧恢复，并统一补偿后续字幕、素材和特效。给持续前进的源视频静音或盖定格图不算暂停。导演备注只写进分镜，不得成为观众画面文案。

### 必须按语义触发的镜头

1. 每一个小标题或章节转场使用 `aurora-bloom-bg-flip`。新标题在翻暗之后出现，并留出稳定阅读时间；不要在同一拍叠加第二个主转场。
2. 口播提到 Skill、工具或工具链名称时使用 `assemble-then-type-flyin`。右侧画框使用该工具的公开 landing page、GitHub 仓库首页或官方 Logo 截图。第一次出现可以完整组装，后续重复出现复用同一工具卡或做短召回，不重复制造全套身份动画。
3. 口播进入“第一步/第二步”等明确步骤时，使用 `autolayout-gap-dial` 串联步骤名。步骤标签必须来自逐字稿，不能用动效虚构产品参数。
4. 开头的结论句或主张句（例如“这个目前你能够最便捷地使用上的超强工作助手”）全屏使用 `logo-shrink-wordmark-lockup`，并放入真实、已授权的产品商标或字标。随后保持短暂停顿或承接画面，接入约 4.5 秒的 `card-flock-tumble` 作为桥接，可配有授权的背景音乐并再次给出真实品牌标志。

### 开头可选叠加层

前几秒可根据口播节奏选择，不要求每条视频全部使用：

- `graze-face-tour`：第一次提到产品名时，使用真实产品官网首页截图作为 UI 地形。
- `gradient-word-sweep`：作为透明或可验证遮罩的关键词层，保留人物脸部和字幕可见。
- `neon-triple-marquee`：放在人物和环境背景之间，默认文字为 `人物、字幕、信息、画面和这些动效`，降低亮度让位给人物。
- `flying-words`：透明或可验证遮罩的关键词纵深层，放在人物身后，不抢字幕和主体。

`gradient-word-sweep`、`neon-triple-marquee` 和 `flying-words` 的 Gallery 版本带有特定背景假设；降低不透明度不等于透明。若准确 demo 输出的是实心背景，必须改成隔离的 alpha/mask 层，并在小样中确认人物、字幕和背景层级没有穿帮。

### 信息与网站展示

- 口播给出可复制的提示词、代码、命令、配置或完整文字时，使用 `document-typewriter-reveal`；原文逐字保留，按语义分页，字幕不能覆盖文档正文。
- 展示真实产品网站时，可参考 `runway-ground-skim`（卡片群落地并转正）或 `skeleton-reveal`（草图/骨架到真实 UI）。每个镜头只选一个主运动语言，真实截图或录屏落定后停止持续运镜。

以上默认结构仍需服从“一镜一个主运动语言”、素材优先全屏、真人退到安全角小窗、字幕属于主画面和高能动作后留稳定阅读时间等硬规则。

### 人物构图与素材转场

每期包装方案除了动效映射，还必须设计一条“人物构图与转场轨”。具体提示词和参考机制见 [`skills/video-packaging-structure/references/talking-head-layout-motion.md`](skills/video-packaging-structure/references/talking-head-layout-motion.md)。默认可在以下状态间按语义切换：

- 人物全屏：开场、观点、情绪重音和返回观众。
- 左侧或右侧人物栏：步骤、列表、概念拆解和右侧素材讲解；人物建议占宽 25%–32%，优先保留手势。
- 安全角 PIP：真实网页、录屏、文档和表格成为主画面时；人物建议占宽 22%–25%，位置由 UI 空白区决定，不强制永远右下。
- 人物暂时隐藏：密集阅读、小标题、概念特效或品牌桥接；结束后沿进入轨迹召回人物。

人物变化只发生在明确的语义状态改变处。同一段口播应复用连续人物视频和音轨，通过裁剪、遮罩、缩放、位置、圆角和层级完成重构；不能重新起片段制造假连续。人物和素材不要同时大幅运动，主素材落定后停止持续运镜并留 1–2 秒阅读时间。

## 过程中可选的动效

### 切屏与节奏

- `shot-transitions · flash-cut`：短促强调、同主题快速切换或返回真人。
- `paper-plane-messenger`：消息传递、发送、交接和分享。
- `overhead-camera-moves · tilt-reveal`：从真人或标题舞台揭示桌面、界面或完整素材。
- `mosaic-reframe`：多张截图、多个阶段或从总览切到单项素材。
- `line-carry-transition`：用同一条线索把标题、截图重点和下一镜连接起来。
- `speed-ramp-freeze · speed-ramp`：快速操作、结果瞬间出现或高能演示；阅读型录屏不要使用。

### 标题、产品名和章节

- `spotlight-hero-card`：产品名、核心命题或章节主卡。
- `segmented-thumb-hero`：多模式、多选项或前后状态切换。
- `type-and-filter`：搜索、筛选、输入关键词后得到结果。
- `cube-navigation`：章节或空间层级切换。
- `card-flock-tumble`：多卡片、多成果或信息集合进入下一段。

标题段统一使用黑灰科技动态网格作为舞台，标题在主画面出现，真人缩到右下角。背景由用户提供时放入 `01-原始素材/07-背景与动效素材/`；由 Codex 生成时写入 `04-动效与包装/背景/`。

### 录屏、UI 和生成过程

- `ai-stream-response`：AI 先给结论，再逐条补证据和完成状态。
- `autolayout-gap-dial`：参数、间距、布局或数值调节。
- `glass-pill-dictation-typing`：语音输入、听写、实时打字和提示词输入。
- `canvas-materialize-moves · panel-to-canvas`：列表、面板或生成结果迁移到开放画布。

录屏和截图必须优先保证可读性。素材铺满全屏后，停止无意义的持续漂移和缩放；只有操作焦点变化时才移动镜头。

### 代码、提示词和指令框

只有口播真的给出可复制文本时才使用 `code-prompt-stage`；如果只是提到“写代码”“输入提示词”而没有给出原文，继续使用标题、录屏或真人状态，不虚构示例。

- 采用深色科技背景加高对比大面板。面板放在左侧或中部，建议占画面宽度 62%–66%；真人放在右侧安全区小窗，宽度约 22%–25%；两栏至少留 3% 间距，画面四周保留 4%–6% 安全边距。
- 面板顶部可以有内容类型标签（如 `BASH`、`CODE`、`PROMPT`）、一句短标题和复制图标/“复制指令”提示，但不能挤占正文。代码和命令使用等宽字体；中文提示词与长指令使用清晰的界面无衬线字体。
- 逐字保留项目提供的原文，包括缩进、换行、标点、大小写、参数、引号和占位符。未经用户确认不得补写、润色或改写可复制内容。
- 8 行以内使用单页稳定展示；9–16 行按语义拆成 2–3 页；超过 16 行只展示关键片段，并在项目分镜中标明完整文本的已确认去向。不得靠缩小字号把长内容硬塞进一屏。
- 入场用 12–18 帧完成边框延展、面板展开和真人缩入安全区；随后至少稳定 1.5 秒。朗读过程中只高亮当前行或短语，不连续滚动，不让整段文字逐字打出。
- 代码框与字幕必须分区。优先预留独立的底部字幕安全带；若版式无法同时保证代码、真人和字幕可读，先移动或缩小真人，再对代码按语义分页，不能让字幕压住可复制文本。
- 同一段连续代码或提示词分页时仍属于 `code-prompt-stage`，可用边框延续或短促换页；结束后再用 `line-carry-transition`、`glow-wake-sleep-panel` 或短促 `flash-cut` 返回录屏或真人。

这一状态的重点是“观众能看清并复用”，不是模拟编辑器操作。避免装饰性假代码、长段持续滚动、细小字号和阅读时的高能运镜。

### 对话与协作组合

讲“你与 AI 对话”“多人协作”或“内容从对话进入画布”时，可以组合：

1. `collab-cursor-moves · dialogue-duet` 表现双方对话、靠近、交接和角色关系。
2. 叠加或接续 `canvas-materialize-moves · panel-to-canvas`，把对话结果变成画布卡片。

组合时仍遵守“一镜一个主运动语言”：先让光标完成对话与交接，再让内容物化，不把两个高能动作同时堆在一个阅读时刻。

### 收束与返回真人

- `spotlight-sweep-moves · glow-wake-sleep-panel`：让当前面板亮起、完成讲解后休眠，再返回真人。
- `line-carry-transition`：让界面中的线、边框或高亮延续到真人画面。
- `shot-transitions · flash-cut`：短促结束操作段或进入总结。

## 切屏硬规则

1. 画面状态发生变化时必须有转场；同一状态内删除口误和卡顿可以使用干净跳切。
2. 先根据逐字转录找到语义落点，再决定切屏帧；不能先铺特效再硬套口播。
3. 每个镜头只使用一种主运动语言，装饰光效不能抢过内容。
4. 高能动效完成后必须留稳定阅读时间；正在阅读录屏时不做持续运镜。
5. 展示录屏、截图、图片和文档时，素材优先全屏，真人退到右下角。
6. 给出可复制的代码、提示词、命令或指令时，进入 `code-prompt-stage`，逐字使用已提供的原文并保证可读性。
7. 标题、产品名和口播字幕都属于主画面，不放在真人副屏内。
8. 同一特效不要连续重复；按语义从特效库中选择，不按固定时间间隔堆效果。
9. 任何小样、完整包装和最终渲染都分别遵守 `workflow.json` 的确认点。

## Codex 执行产物

进入动效阶段后，Codex 先在当前项目的 `04-动效与包装/` 生成：

- `特效映射.json`：口播时间、语句、画面状态、素材路径、镜头卡、style-key、转场和字幕避让。
- `导演分镜.md`：逐段说明主画面、真人位置、动效目的、稳定阅读时长和声音提示。
- `背景/`：项目生成的科技网格或其他可复用背景。

未经小样批准，不继续完整包装；未经最终渲染批准，不写入 `07-成片/`。
