# Codex 视频工作流入口

当用户提出视频剪辑、口播精剪、字幕、Remotion 动效、Shotcraft 特效、视频工作流安装，或要使用本仓库制作视频时：

1. 先读取 `00-先读我.md`、`workflow.json` 和 `dependencies.json`；涉及剪辑、动效、包装、字幕或切屏时，再读取 `effects.json` 和 `特效与切屏规范.md`。
2. 设置或排查环境时，读取 `.agents/skills/video-production-bootstrap/SKILL.md`，先运行只读检查。
3. 每条独立视频使用一个项目目录。若用户尚未指定项目，按 `workflow.json` 调用 `create_video_project.py` 新建；不要复用其他视频的工作目录。
4. 除本工作流明确列出的规范文件外，运行时只读取当前项目的 `project.json` 与 `01-原始素材/`；所有中间产物和成片写入该项目的阶段目录，绝不覆盖 `01-原始素材/`。
5. 只有用户明确要求安装时，才能下载外部 Skill 或修改本机环境；安装授权不等于剪辑或渲染授权。
6. 视觉设计按需读取 `skills/ai-visual-director/SKILL.md`；转录与精剪按清单调用相应 Skill。每次画面状态变化必须有语义匹配的转场，素材全屏时真人退到安全角小窗，字幕始终属于主画面。
7. 不读取、提交或上传 `.env`、凭据、个人项目、真人原片、私人录屏、私密转录、缓存和渲染产物。
8. “先看小样”“禁止直接做视频”“不允许 render”等确认点必须原样执行，不能因为工具已经安装而跳过。
