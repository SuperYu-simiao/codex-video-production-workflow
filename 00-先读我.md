# 视频工作流：先读我

这个本地项目集中保存“如何让 Codex 帮你做视频”的操作框架、个人视频项目、素材、动效与本机 Skills。个人内容均由 Git 忽略，默认只在本机使用。

## 第一次在本机使用

1. 在克隆后的工作流根目录打开 Codex。
2. 让 Codex 读取 `dependencies.json` 并运行只读检查。
3. 只有确认缺失项后，再授权安装外部 Skills 或系统工具。
4. 不要把 API Key 粘贴进聊天；只在本机设置环境变量。

克隆仓库后要让 Codex 自动发现完整剪辑入口，可以执行：

```bash
python3 .agents/skills/video-production-bootstrap/scripts/install_workflow.py --all
```

它会链接 `$video-editing-workflow`，并按 `dependencies.json` 安装固定版本的 `video-use` 与 `video-shotcraft`。只接入仓库内入口时使用 `--install`；两种模式都不会触碰视频素材、凭据或成片。

推荐提示词：

```text
请使用 $video-production-bootstrap 检查这个本地项目的视频环境。
先只报告缺失项，不要安装、创建项目、剪辑或渲染。
```

完整剪辑时使用：

```text
请使用 $video-editing-workflow 完成这条视频。
先按 video-use → video-shotcraft → Remotion 工作，读取当前项目的 project.json 和 01-原始素材/。
先输出包装方案、补录清单、官网/GitHub 截图表、特效映射和导演时间线，不要直接渲染。
```

## 什么时候新建项目文件夹

以下任一情况都要新建项目：

- 开始一条全新且独立的视频；
- 更换主要口播、主题或最终交付物；
- 需要保留上一条视频的转录、剪辑决策和动效工程；
- 用户明确说“新建项目”或“开始一条新视频”。

同一条视频的补录、补图和修改继续使用原项目。不要为了一个小修改再建项目，也不要把两条独立视频塞进同一目录。

新建命令：

```bash
python3 .agents/skills/video-production-bootstrap/scripts/create_video_project.py \
  --name "产品功能演示"
```

## 素材放在哪里

项目创建后，只把原始输入放进 `01-原始素材/`：

- `01-口播视频/`：相机原片、手机口播、补录片段；
- `02-录屏/`：产品操作、网页、软件或教程录屏；
- `03-图片与截图/`：产品图、截图、照片和已获授权图片；
- `04-音频/`：独立录音、旁白、已获授权音乐和音效；
- `05-文稿与字幕/`：口播稿、SRT、文字稿和已有转录；
- `06-授权说明/`：素材来源、许可证、可公开范围和署名要求。
- `07-背景与动效素材/`：用户提供的动态背景、授权 B-roll 和外部动效素材。

不要把原始素材放进 `02-转录与剪辑决策/` 之后的目录；那些目录由 Codex 和剪辑工具写入。

## Codex 从哪里读取

开始剪辑时，明确告诉 Codex 项目名或项目路径。Codex 按下面顺序读取：

1. 当前项目的 `project.json`；
2. `01-原始素材/05-文稿与字幕/` 中已有的转录或字幕；
3. `01-原始素材/01-口播视频/`；
4. 其余录屏、图片、音频和授权说明；
5. `effects.json` 与 `特效与切屏规范.md`；
6. 已经确认过的中间产物。

仓库根目录是视频相关内容的本地总文件夹；旧视频、旧项目、动效库和成片继续在这个总文件夹内原位保存并由 Git 忽略，不为某次剪辑迁移或清空。运行时只读取用户明确指定、且含 `project.json` 的当前视频项目，不枚举其他项目、旧项目或整个动效库。用户另行给出绝对路径时，只读取该次操作明确指定的文件。

## 高效本地工具

三个工具都要求显式指定当前项目。生成代理、从已有预览抽帧、检查进程是彼此独立的操作，不代表用户批准剪辑、启动 Remotion、渲染成片或结束进程。

短代理默认只打印计划；只有添加 `--execute` 才从原片的明确区间生成低分辨率代理，并同时写入可回连原片的 JSON：

```bash
python3 .agents/skills/video-production-bootstrap/scripts/create_short_proxy.py \
  --project '/绝对路径/当前项目' \
  --input '/绝对路径/原片.mp4' \
  --start 00:01:20.500 \
  --end 00:01:45.000
```

批量抽帧只接受已经存在的视频预览，一次 FFmpeg 进程完成多个时间点；预览不存在时直接报错，不会启动 Remotion 或补渲整片：

```bash
python3 .agents/skills/video-production-bootstrap/scripts/batch_extract_frames.py \
  --project '/绝对路径/当前项目' \
  --input '/绝对路径/已存在预览.mp4' \
  --time 1.5 --time 8.25 --time 19
```

残留进程检查严格限定当前项目路径，只列出疑似渲染进程。本工具没有终止功能：

```bash
python3 .agents/skills/video-production-bootstrap/scripts/check_render_processes.py \
  --project '/绝对路径/当前项目'
```

## Git、文件监听与构建排除

- `.gitignore` 只阻止 Git 收集本地项目、旧项目、动效库、原始素材、代理、转录缓存、审核帧、成片、依赖和构建产物；它不控制任何 watcher。
- 仓库根 `.vscode/settings.json` 使用 VS Code 原生 `files.watcherExclude` 和 `search.exclude`，避免打开总文件夹时扫描本地重目录。编辑当前视频时，直接打开它的 `05-Remotion工程/`，新项目内也有独立的 VS Code 排除配置。
- 新项目的 `05-Remotion工程/remotion.config.ts` 使用 Remotion 官方支持的 `Config.overrideWebpackConfig()` 与 Webpack `watchOptions.ignored`。它只排除当前项目内的原始素材、缓存、代理、审核与成片目录。
- Node.js 没有通用 watcher 排除开关。只从当前项目的 `05-Remotion工程/` 启动具体工具；如果以后引入 Vite、Next、nodemon 等独立 watcher，再使用该工具实际支持的配置，不伪造通用设置。
- 共享 `动效库/` 位于当前 Remotion 工程之外，不扫描、不整库 import；只允许引用用户为当前项目明确选定的内容。未引用内容不会进入 Remotion/Node 的依赖图。

## 标准流程

1. 素材盘点：确认数量、格式、时长、分辨率和授权。
2. 转录与精剪方案：优先复用已有转录，标出口误、卡顿和保留范围。
3. 口播精剪：保留原片，生成新的精剪文件和剪辑决策。
4. 动效与包装方案：读取 [`skills/video-packaging-structure/SKILL.md`](skills/video-packaging-structure/SKILL.md)，按 [`特效与切屏规范.md`](特效与切屏规范.md) 先出包装方案、补录清单、公开截图素材表和特效映射，不直接整片渲染。
5. Remotion/动画制作：每种实现放在当前项目自己的工程目录。
6. 预览与审核：输出低成本预览、关键帧和检查记录。
7. 用户批准后成片：渲染最终文件并做时长、画面、字幕和音频 QA。

### 包装参数编辑器

需要自己反复调整包装时，可以启动仓库根目录的 `tools/remotion-editor/`：

```bash
cd tools/remotion-editor
npm run dev
```

它是基于 `@remotion/player` 的本地预览和参数面板，不是完整剪映时间线。它可以切换素材、标题、人物布局、背景/素材槽位、转场、特效强度、PIP 大小和场景时间；配置保存到当前视频项目的 `05-Remotion工程/config/video.config.json`。它不会修改 `01-原始素材/`，也不代表获得预览或最终渲染授权。

“安装完成”“素材已放好”“方案通过”和“允许渲染”是不同授权，不能互相替代。

## 每期默认包装结构

如果用户没有针对本期另行指定视觉规则，默认使用 `video-packaging-structure`：小标题用 `aurora-bloom-bg-flip`，工具/Skill 名称用 `assemble-then-type-flyin`，步骤名用 `autolayout-gap-dial`，开头结论句用 `logo-shrink-wordmark-lockup` 后接 `card-flock-tumble` 桥接；开头透明叠加层、文档文字展示和产品网站镜头按语义选择。每期必须先写完 `04-动效与包装/` 下的四份规划文档，获得对应阶段确认后才进入下一步。
