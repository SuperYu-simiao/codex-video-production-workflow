# 视频工作流：先读我

这个公开包提供“如何让 Codex 帮你做视频”的操作框架，不附带作者的视频素材和成片。

## 第一次下载后

1. 在本仓库根目录打开 Codex。
2. 让 Codex 读取 `dependencies.json` 并运行只读检查。
3. 只有确认缺失项后，再授权安装外部 Skills 或系统工具。
4. 不要把 API Key 粘贴进聊天；只在本机设置环境变量。

推荐提示词：

```text
请使用 $video-production-bootstrap 检查这个仓库的视频环境。
先只报告缺失项，不要安装、创建项目、剪辑或渲染。
```

## 什么时候新建项目文件夹

以下任一情况都要新建项目：

- 开始一条全新且独立的视频；
- 更换主要口播、主题或最终交付物；
- 需要保留上一条视频的转录、剪辑决策和动效工程；
- 用户明确说“新建项目”或“开始一条新视频”。

同一条视频的补录、补图和修改继续使用原项目。不要为了一个小修改再建项目，也不要把两条独立视频塞进同一目录。

新建命令：

```bash
python3 .agents/skills/video-production-bootstrap/scripts/create_video_project.py \
  --name "产品功能演示"
```

## 素材放在哪里

项目创建后，只把原始输入放进 `01-原始素材/`：

- `01-口播视频/`：相机原片、手机口播、补录片段；
- `02-录屏/`：产品操作、网页、软件或教程录屏；
- `03-图片与截图/`：产品图、截图、照片和已获授权图片；
- `04-音频/`：独立录音、旁白、已获授权音乐和音效；
- `05-文稿与字幕/`：口播稿、SRT、文字稿和已有转录；
- `06-授权说明/`：素材来源、许可证、可公开范围和署名要求。
- `07-背景与动效素材/`：用户提供的动态背景、授权 B-roll 和外部动效素材。

不要把原始素材放进 `02-转录与剪辑决策/` 之后的目录；那些目录由 Codex 和剪辑工具写入。

## Codex 从哪里读取

开始剪辑时，明确告诉 Codex 项目名或项目路径。Codex 按下面顺序读取：

1. 当前项目的 `project.json`；
2. `01-原始素材/05-文稿与字幕/` 中已有的转录或字幕；
3. `01-原始素材/01-口播视频/`；
4. 其余录屏、图片、音频和授权说明；
5. `effects.json` 与 `特效与切屏规范.md`；
6. 已经确认过的中间产物。

Codex 不应扫描其他项目、作者的个人视频目录或仓库外的任意文件夹。用户另行给出绝对路径时，只读取用户明确指定的文件。

## 标准流程

1. 素材盘点：确认数量、格式、时长、分辨率和授权。
2. 转录与精剪方案：优先复用已有转录，标出口误、卡顿和保留范围。
3. 口播精剪：保留原片，生成新的精剪文件和剪辑决策。
4. 动效与包装方案：按 [`特效与切屏规范.md`](特效与切屏规范.md) 先出分镜、素材映射和小样，不直接整片渲染。
5. Remotion/动画制作：每种实现放在当前项目自己的工程目录。
6. 预览与审核：输出低成本预览、关键帧和检查记录。
7. 用户批准后成片：渲染最终文件并做时长、画面、字幕和音频 QA。

“安装完成”“素材已放好”“方案通过”和“允许渲染”是不同授权，不能互相替代。
